---
description: Indra Desktop Pet 项目级总则：文档同步、中文注释、协作约定
alwaysApply: true
---

# Indra Desktop Pet — 项目总则

## 项目上下文

- FGO 因陀罗桌面宠物，Python 3.10+ / PySide6，入口 `python src/main.py`
- 权威文档在 `docs/`（索引见 `docs/README.md`），改代码前先查阅相关文档
- 开发环境 API 密钥走根目录 `.env`（不提交 Git）；打包版仅 `config/settings.json`

## 功能完成后的文档同步（必须）

每次**跑通一个功能**后，在声称完成前检查并更新 `docs/` 中相关文档：

| 变更类型 | 需更新的文档 |
|----------|--------------|
| 新功能 / 完成 Todo | `docs/04-开发进度.md` |
| 新模块、数据流、线程模型 | `docs/03-程序架构.md` |
| 新依赖、API、环境要求 | `docs/02-技术要点.md` |
| 新配置项 / DB 字段 | `docs/07-数据结构.md` + `SettingsManager.DEFAULTS` |
| 开发/部署步骤变化 | `docs/05-开发环境配置.md` |
| 产品范围变化 | `docs/06-产品需求(PRD).md` → `docs/08-实现计划.md` |
| 分阶段开发步骤 | `docs/08-实现计划.md` |

只更新与本次改动**直接相关**的章节，避免无关文档大改。

## 中文注释（必须）

- 在**关键位置**用**简体中文**写注释，保持可维护性
- **应注释**：模块职责、跨线程/跨模块边界、非显而易见的业务规则、workaround、与 LLM/RAG/记忆相关的约束
- **不必注释**：显而易见的 getter/setter、单行 UI 布局、已自解释的变量名
- 每个 `.py` 文件顶部保留模块级 docstring（中文，说明职责）
- 复杂类/公共方法用 docstring；分段逻辑可用 `# ========== 标题 ==========`（与现有代码一致）

## 通用开发原则

- **最小改动**：只改任务相关代码，不顺手重构无关模块
- **匹配现有风格**：命名、导入方式、SettingsManager 单例、延迟 import 防循环依赖
- **禁止提交密钥**：勿将 API Key 写入代码或 `settings.json` 并提交；`.env` 已在 `.gitignore`
- **路径统一**：资源与配置路径用 `utils.resource_path()`，兼容 PyInstaller
- **耗时操作**：网络请求、截图、索引加载放 `QThread` 或 `threading`，不阻塞 Qt 主线程

## 回复语言

与用户沟通、PR/提交说明使用**简体中文**（除非用户明确要求英文）。
