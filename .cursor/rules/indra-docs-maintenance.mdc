---
description: docs/ 文档维护清单（功能落地后同步更新）
globs: docs/**/*.md
alwaysApply: false
---

# 文档维护规范

编辑或新增 `docs/` 内容时，保持与代码一致，并遵循现有文档结构。

## 文档地图

| 文件 | 何时更新 |
|------|----------|
| `01-项目概况.md` | 产品定位、目录结构重大变化 |
| `02-技术要点.md` | 依赖、API、本地资源、打包方式 |
| `03-程序架构.md` | 新模块、数据流、线程模型 |
| `04-开发进度.md` | 功能完成/待办状态变化 |
| `05-开发环境配置.md` | `.env`、安装步骤、首次运行 |
| `06-产品需求(PRD).md` | 需求范围、用户场景 |
| `07-数据结构.md` | settings schema、知识库约定、DB 表结构 |
| `08-实现计划.md` | 开发阶段与步骤 |

## 功能完成检查清单

功能**跑通并验证**后，完成方应：

1. 在 `04-开发进度.md` 勾选或移动对应条目（已完成 / 待办）
2. 若涉及架构或配置 schema，更新 `03` 或 `07`
3. 若新增开发步骤，更新 `08-实现计划.md` 对应阶段
4. 产品行为与 PRD 不一致时，先改 `06`，再改实现与进度

## 写作要求

- 使用简体中文，表格与 mermaid 图保持简洁
- 配置字段名、路径、类名与代码**字面一致**
- 不写已废弃的实现；迁移说明可保留一小节「历史变更」
- 根目录 `PRD.md` 等 stub 仅作跳转，正文只维护 `docs/` 内文件

## 与 Cursor Rules 的关系

代码规范见 `.cursor/rules/indra-project-core.mdc` 与 `indra-python-standards.mdc`；本文档规则在直接编辑 `docs/` 时生效。
