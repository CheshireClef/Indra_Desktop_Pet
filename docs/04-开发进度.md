# 开发进度

> 与 [06-产品需求(PRD)](./06-产品需求(PRD).md) 功能表、[08-实现计划](./08-实现计划.md) 对照维护。  
> 最后更新：2026-05-26

---

## 已完成

### 基础交互
- [x] 桌面透明无边框窗口、拖拽、置顶、滚轮缩放
- [x] 系统托盘图标与右键菜单（显示/隐藏、设置、退出）
- [x] 双击立绘打开气泡式聊天窗口
- [x] 聊天气泡字体大小可调

### 智能对话
- [x] LLM 对话：OpenAI 兼容 API（base_url / api_key / model / temperature / max_tokens / history_rounds）
- [x] LLM 回复 JSON 结构化：`reply` / `emotion`（记忆由 `MemoryExtractWorker` 单独抽取）
- [x] 解析失败回退至情绪标签剥离；空回复红色临时气泡
- [x] 根据回复情绪自动切换表情/Emoji

### RAG 知识库
- [x] lore（摩诃婆罗多分卷、FGO 剧情）+ style（语气）双索引
- [x] 本地向量嵌入：`gte-multilingual-base` 离线运行
- [x] 文件 manifest 增量检测与索引重建
- [x] 数据库加载进度/失败 Signal 提示
- [x] 嵌入并发安全：单锁 + 就绪/重建门控、ChatManager RAG 降级、query 截断（修复加载中聊天 IndexError）

### 长期记忆
- [x] SQLite + 向量（`config/user_memory.db`），开关默认关闭
- [x] 检索注入 system；记忆写入由 MemoryExtractWorker / 屏幕抽取（memory 模型）负责
- [x] 去重、冲突更新、同主题聚类与 LLM 合并（5 条倍数触发）
- [x] 混合评分检索：语义相似度 + 艾宾浩斯时间衰减（半衰期 30 天）
- [x] 设置页「记忆管理」：列表、编辑、单条删除、清空（二次确认）
- [x] 屏幕观察描述也可抽取长期记忆
- [x] 双路检索 + `ref_count` 强化 + `pinned` 永久保留
- [x] 设置页「整理全部记忆」：预览 → 确认 → Worker 执行（合并 + 规则删除，无后台定时任务）

### 屏幕观察
- [x] 定时截图 → Qwen-VL 描述 → LLM 带情绪评论 → 临时气泡
- [x] 截图数量上限可配置；Worker 线程不阻塞 UI

### 模型配置升级（v2）
- [x] `models` schema：vendor 注册表 + 动态 `/v1/models` + 能力缓存
- [x] 设置页：对话模型 / 识图模型各自含 API 连接；识图可勾选与对话共用（灰显镜像）或独立配置；测试连接、刷新列表、测试识图
- [x] JSON 输出 `auto` 策略链（response_format → prompt → 自然语言）
- [x] 屏幕观察空描述友好提示；`extract_message_content` 兼容推理字段
- [x] 修复 SiliconFlow Qwen3.5/3.6 等识图探测误报：关闭 `enable_thinking`、提高探测 token、增强多模态响应解析
- [x] 识图通用适配层：vendor `vision_defaults`、64px 探测图、自适应重试、`vision_hints` 缓存（修复 GLM-4.5V 等最小图尺寸校验）
- [x] 聊天长期记忆后台抽取：`models.memory`、记忆管理页 API 配置、`MemoryExtractWorker`、`memory_extract.md`；主聊天 JSON 仅 reply/emotion
- [x] LLM 通信层梳理：安全 JSON 解析、解析失败重试、不写脏 history；`chat_pipeline` / `parsers`；屏幕记忆改走 memory 模型
- [x] 聊天气泡异步：`ChatWorker` 后台调用 LLM，用户消息发送即显示，等待期间可滚动/关闭窗口
- [x] 配置热更新清空 `ModelService` / vision 客户端缓存

### 配置与工程
- [x] `config/settings.json` 持久化 + SettingsManager 热更新
- [x] 设置对话框：基础 / 模型 / 记忆管理
- [x] 开发环境 `.env` 加载 API 密钥（2026-01 提交 `9ec3dac`）
- [x] PyInstaller 打包配置（`FGO因陀罗桌宠.spec`）
- [x] 打包路径分离：`resource_path`（只读 _internal）与 `user_data_path`（exe 旁可写）；`打包.bat` + 打包前后校验脚本
- [x] 项目文档体系（`docs/` 目录）

---

## 进行中

- 无

---

## 待开发（Todo）

- [ ] **戳一戳互动动画**：单击/戳动立绘触发专用动画（`_trigger_poke` 已有占位）
- [ ] **闲置待机动画**：打瞌睡、随机漫游等，配合 `idle_interval_s`
- [ ] **好感度等游戏性功能**：可复用 JSON 中 `favorability_delta` 预留字段
- [ ] **长期记忆后续优化**（整理时 LLM 关系判重、删除阈值可配置等）
- [ ] **（可选）持久化**：聊天记录、截图与屏幕评论的长期保存与检索

---

## 已知问题

- 桌宠偶发消失：托盘「显示桌宠」或先隐藏再显示可恢复（见根目录 README FAQ）

---

## 里程碑参考（Git）

| 时期 | 代表提交/主题 |
|------|---------------|
| 动画与 RAG 重构 | 分离 worker、RAG 从 chat_manager 独立 |
| 打包与体积优化 | gte-multilingual-base 替换，体积约 500MB |
| JSON 结构化 + 长期记忆 | `79989e8` 起 |
| 混合评分与 .env 开发配置 | `bce19c3`、`9ec3dac` |
